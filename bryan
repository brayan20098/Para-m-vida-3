<!DOCTYPE html><html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Te Amo, mi Nubecita de Azúcar</title>
  <style>
    html, body {
      margin: 0;
      padding: 0;
      overflow: hidden;
      background: #ffe6f0;
      font-family: 'Comic Sans MS', cursive;
    }
    h1 {
      position: absolute;
      top: 40%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 3rem;
      color: #ff4da6;
      text-align: center;
      text-shadow: 2px 2px 5px #fff;
    }
    .heart {
      width: 20px;
      height: 20px;
      position: absolute;
      background-color: red;
      transform: rotate(45deg);
      animation: fall 5s linear infinite;
    }
    .heart::before,
    .heart::after {
      content: '';
      width: 20px;
      height: 20px;
      background-color: red;
      border-radius: 50%;
      position: absolute;
    }
    .heart::before {
      top: -10px;
      left: 0;
    }
    .heart::after {
      left: -10px;
      top: 0;
    }
    @keyframes fall {
      0% {top: -10%; opacity: 1;}
      100% {top: 110%; opacity: 0;}
    }
    .explosion {
      position: absolute;
      pointer-events: none;
    }
  </style>
</head>
<body>
  <h1>Te amo, mi nubecita de azúcar ☁️💕</h1>  <script>
    // Corazones que caen
    function createFallingHeart() {
      const heart = document.createElement('div');
      heart.className = 'heart';
      heart.style.left = Math.random() * 100 + 'vw';
      heart.style.animationDuration = (Math.random() * 3 + 2) + 's';
      document.body.appendChild(heart);
      setTimeout(() => heart.remove(), 5000);
    }
    setInterval(createFallingHeart, 200);

    // Explosión de corazones al tocar/clic
    function explosion(x, y) {
      for (let i = 0; i < 20; i++) {
        const heart = document.createElement('div');
        heart.className = 'heart explosion';
        heart.style.left = x + 'px';
        heart.style.top = y + 'px';
        heart.style.width = '10px';
        heart.style.height = '10px';
        heart.style.transition = 'transform 0.6s ease-out, opacity 0.6s';
        document.body.appendChild(heart);

        setTimeout(() => {
          heart.style.transform = `translate(${(Math.random() - 0.5) * 200}px, ${(Math.random() - 0.5) * 200}px)`;
          heart.style.opacity = 0;
        }, 0);

        setTimeout(() => heart.remove(), 700);
      }
    }

    document.addEventListener('click', e => {
      explosion(e.clientX, e.clientY);
    });
    document.addEventListener('touchstart', e => {
      const touch = e.touches[0];
      explosion(touch.clientX, touch.clientY);
    });
  </script></body>
</html>